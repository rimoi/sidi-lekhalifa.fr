{{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="pull-right">
        <button class="btn btn-success admin-margin">{{ button_label|default('Enregistrer') }}</button>
    </div>
{{ form_end(form) }}
